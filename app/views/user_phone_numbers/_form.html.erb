<%= form_for(user_phone_number) do |f| %>
  <% if user_phone_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_phone_number.errors.count, "error") %> prohibited this user_phone_number from being saved:</h2>

      <ul>
      <% user_phone_number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %>
    <%= f.text_field :number %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
