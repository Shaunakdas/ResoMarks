<%= form_for(exam_attempt) do |f| %>
  <% if exam_attempt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam_attempt.errors.count, "error") %> prohibited this exam_attempt from being saved:</h2>

      <ul>
      <% exam_attempt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :exam_id %>
    <%= f.text_field :exam_id %>
  </div>

  <div class="field">
    <%= f.label :exam_set_id %>
    <%= f.text_field :exam_set_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
